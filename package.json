{
  "name": "loop54-js-lib",
  "version": "0.2.0",
  "description": "JS lib for Loop54 easy frontend implementation.",
  "main": "lib/index.js",
  "scripts": {
    "compile": "npm run babelify && npm run browserify && npm run test",
    "babelify": "./node_modules/.bin/babel src -d lib",
    "browserify": "browserify src/browser.js -o lib/loop54-js-lib.js -t [ babelify --presets [ es2015 stage-2 ] ]",
    "test": "mocha --require mocha-babel.js ./test/lib-test.js"
  },
  "test": "mocha --require mocha-babel.js ./test/lib-test.js",
  "keywords": [
    "Loop54",
    "SaaS",
    "Search"
  ],
  "author": "Loop54",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.7.0"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-core": "^6.2.1",
    "babel-preset-es2015": "^6.1.18",
    "babel-preset-stage-2": "^6.1.18",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1",
    "chai": "^3.4.1"
  }
}
